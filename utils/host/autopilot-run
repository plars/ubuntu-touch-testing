#!/bin/sh


UTAH_PROBE_DIR="${UTAH_PROBE_DIR-/var/cache/utah-probes}"

adb-shell sudo -i -u phablet bash -ic \"UTAH_PROBE_DIR=$UTAH_PROBES_DIR PYTHONPATH=/home/phablet/autopilot autopilot run -v $*\"
